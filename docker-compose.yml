version: '3.1'

services:

  db:
    image: postgres
    restart: always
    environment:
      POSTGRES_USERNAME: postgres_user
      POSTGRES_PASSWORD: postgres_pass
      POSTGRES_DB: demo
    ports:
    - 5432:5432

#  adminer:
#    image: adminer
#    restart: always
#    ports:
#      - 8080:8080